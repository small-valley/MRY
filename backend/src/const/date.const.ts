// getDay() returns 0 for Sunday, 1 for Monday, and so on.
// However, getDay() returns result based on local time zone.
// Therefore, if the backend app runs on Vancouver time zone, getDay() returns 0 for Monday.
export const MONDAY = 1;
export const FRIDAY = 5;
